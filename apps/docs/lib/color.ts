/* The color maths moved to the create-forte-ui package, which needs the same
 * model to emit measured on-colors and seed warnings — one module of record.
 * This re-export keeps the docs' `@/lib/color` imports stable. */
export * from "create-forte-ui/color";
